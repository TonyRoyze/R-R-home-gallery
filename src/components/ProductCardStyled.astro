---
// ProductCard.astro
interface Props {
    title: string;
    // price: number;
    // originalPrice?: number;
    // rating: number;
    image: ImageMetadata;
    description: string;
    inStock?: boolean;
}

import Image from "astro/components/Image.astro";

const {
    title,
    // price,
    // originalPrice,
    // rating,
    image,
    description,
    // inStock = true,
} = Astro.props;

// const formatPrice = (amount: number) => {
//     return new Intl.NumberFormat("en-US", {
//         style: "currency",
//         currency: "USD",
//     }).format(amount);
// };

// const stars = Array(5)
//     .fill("")
//     .map((_, index) => index < rating);
---

<div
    class="group relative overflow-hidden border-2 border-amber-800 rounded-lg bg-dutch-white"
>
    <div
        class="relative aspect-square overflow-hidden border-b-2 border-amber-800"
    >
        <div class="absolute inset-0 bg-[var(--color-primary)]/10 z-10"></div>
        <Image
            src={image}
            alt={title}
            class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
            loading="lazy"
        />
    </div>

    <!-- Product details with vintage typography -->
    <div class="p-6 bg-[var(--color-background)]">
        <h3
            class="font-serif text-xl text-[var(--color-primary)] mb-3 leading-relaxed"
        >
            {title}
        </h3>

        <!-- Price section with decorative line
        <div class="flex items-baseline mb-3 border-b border-amber-800/30 pb-3">
            <span class="text-2xl font-serif text-[var(--color-primary)]">
                {formatPrice(price)}
            </span>
            {
                originalPrice && (
                    <span class="ml-2 text-sm text-amber-700/70 line-through font-serif">
                        {formatPrice(originalPrice)}
                    </span>
                )
            }
        </div> -->

        <!-- Rating with antique stars -->
        <!-- <div class="flex items-center mb-3">
            {
                stars.map((filled) => (
                    <svg
                        class={`w-5 h-5 ${filled ? "text-amber-800" : "text-amber-800/20"}`}
                        fill="currentColor"
                        viewBox="0 0 20 20"
                    >
                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
                    </svg>
                ))
            }
            <span class="ml-2 text-sm text-amber-800/70 font-serif">
                {rating.toFixed(1)}
            </span>
        </div> -->

        <!-- Description with aged paper look -->
        <p
            class="text-sm text-[var(--color-primary)]/80 mb-4 font-serif leading-relaxed"
        >
            {description}
        </p>

        <!-- Stock status and Add to cart with vintage button style -->
        <!-- <div
            class="flex items-center justify-between mt-4 border-t border-amber-800/30 pt-4"
        >
            {
                inStock ? (
                    <span class="text-sm text-morning-blue font-serif">
                        Available
                    </span>
                ) : (
                    <span class="text-sm text-red-800 font-serif">
                        Sold Out
                    </span>
                )
            }

            <button
                class={`px-6 py-2 font-serif text-sm transition-all duration-300 border-2 ${
                    inStock
                        ? "border-amber-800 text-[var(--color-primary)] hover:bg-amber-800 hover:text-amber-50"
                        : "border-amber-300 text-amber-300 cursor-not-allowed"
                }`}
                disabled={!inStock}
            >
                Add to Cart
            </button>
        </div> -->
    </div>
</div>
